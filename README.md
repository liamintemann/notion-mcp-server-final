# Notion MCP Server

A lightweight Model Context Protocol server that proxies Notion API calls for OpenAI Agent Builder.

## Commands
- `queryDatabase(databaseId, filter)`

## Deployment
Works on Railway via Dockerfile.
